# Single source of truth for which depth model the whole pipeline uses.
# Change this one line to switch models everywhere - api.py, generate_samples.py, etc.
MODEL_ID = "depth-anything/Depth-Anything-V2-Base-hf"